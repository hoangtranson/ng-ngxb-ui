import { SubTitleDirective } from './sub-title.directive';

describe('SubTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new SubTitleDirective();
    expect(directive).toBeTruthy();
  });
});
