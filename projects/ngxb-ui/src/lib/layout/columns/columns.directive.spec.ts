import { ColumnsDirective } from './columns.directive';

describe('ColumnsDirective', () => {
  it('should create an instance', () => {
    const directive = new ColumnsDirective();
    expect(directive).toBeTruthy();
  });
});
