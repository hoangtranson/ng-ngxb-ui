import { BannerBottomDirective } from './banner-bottom.directive';

describe('BannerBottomDirective', () => {
  it('should create an instance', () => {
    const directive = new BannerBottomDirective();
    expect(directive).toBeTruthy();
  });
});
