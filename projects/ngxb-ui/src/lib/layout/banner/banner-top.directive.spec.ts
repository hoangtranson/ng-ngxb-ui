import { BannerTopDirective } from './banner-top.directive';

describe('BannerTopDirective', () => {
  it('should create an instance', () => {
    const directive = new BannerTopDirective();
    expect(directive).toBeTruthy();
  });
});
