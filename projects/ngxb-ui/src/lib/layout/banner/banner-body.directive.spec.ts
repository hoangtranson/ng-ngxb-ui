import { BannerBodyDirective } from './banner-body.directive';

describe('BannerBodyDirective', () => {
  it('should create an instance', () => {
    const directive = new BannerBodyDirective();
    expect(directive).toBeTruthy();
  });
});
