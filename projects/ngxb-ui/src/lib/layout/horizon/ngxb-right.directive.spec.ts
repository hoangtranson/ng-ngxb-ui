import { NgxbRightDirective } from './ngxb-right.directive';

describe('NgxbRightDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxbRightDirective();
    expect(directive).toBeTruthy();
  });
});
