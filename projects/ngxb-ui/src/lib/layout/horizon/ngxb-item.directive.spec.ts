import { NgxbItemDirective } from './ngxb-item.directive';

describe('NgxbItemDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxbItemDirective();
    expect(directive).toBeTruthy();
  });
});
