import { NgxbLeftDirective } from './ngxb-left.directive';

describe('NgxbLeftDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxbLeftDirective();
    expect(directive).toBeTruthy();
  });
});
