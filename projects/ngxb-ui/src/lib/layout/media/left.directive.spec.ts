import { LeftDirective } from './left.directive';

describe('LeftDirective', () => {
  it('should create an instance', () => {
    const directive = new LeftDirective();
    expect(directive).toBeTruthy();
  });
});
