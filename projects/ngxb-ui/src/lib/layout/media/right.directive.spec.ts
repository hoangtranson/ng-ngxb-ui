import { RightDirective } from './right.directive';

describe('RightDirective', () => {
  it('should create an instance', () => {
    const directive = new RightDirective();
    expect(directive).toBeTruthy();
  });
});
